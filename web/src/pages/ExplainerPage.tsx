/**
 * The tails-problem explainer (DESIGN.md explainer spec; copy in
 * docs/EXPLAINER_COPY.md). This stub reserves the entry; the page content
 * lands in the explainer stage.
 */

export default function ExplainerPage() {
  return (
    <main>
      <h1>The Tails Problem</h1>
      <p>This page is being written — check back shortly.</p>
    </main>
  );
}
