/**
 * Circuits & stats page (DESIGN.md stats spec; dataset at
 * public/data/circuit_stats.json, documented in docs/STATS_NOTES.md). This
 * stub reserves the entry; the page content lands in the stats stage.
 */

export default function StatsPage() {
  return (
    <main>
      <h1>Circuits &amp; Stats</h1>
      <p>This page is being written — check back shortly.</p>
    </main>
  );
}
